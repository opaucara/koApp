   this.myPostProcessingLogic = function(elements, data){
        if(this.foreach()[[this.foreach().length-1]] === data){
          $(".scrollBar").mCustomScrollbar('destroy');
          $(".scrollBar").mCustomScrollbar();
        }
      };

 <ul data-bind="foreach: viewAllEvents() ? eventList : filteredEventList" class="eventList" id="eventsSidebar">
+        <ul data-bind="foreach: {data:(viewAllEvents() ? eventList : filteredEventList),afterRender: myPostProcessingLogic}" class="eventList" id="eventsSidebar">
          

