EG.
the landingPage (listArea) could have 4 states to handle
-0 New
-1 Incomplete/Draft request
-2 Pending request Approval
-3 Requested Ended (approved or Rejected)
viewModule.SelectioListArea[0] = load "standar list module"
viewModule.SelectioListArea[3] = this module will load "Disable list with a Select disaster Icon w modal to completed form"


State machine
Slides
http://code.dougneiner.com/presentations/machina/#session/1
Demo
http://code.dougneiner.com/presentations/machina/connectivity/
*************************
Cool modal efects
http://tympanus.net/Development/ModalWindowEffects/
******************************
LBL_FINANCIAL_FORM = "Financial Form";
LBL_STATUS = 'Status';
LBL_LOSS_TYPE = 'Loss type';

MSG_ERROR_USER_NOT_FOUND = "The user doesn't exist";
MSG_USER_SUCCESSFULLY_DELETED = "User was successfully deleted";
MSG_ERROR_CHOOSE_OPTION = 'Choose an option';
MSG_GENERIC_ERROR = "An error has ocurred";
MSG_SUCCESS = 'Success';
MSG_ERROR = 'Error';
MSG_SUBMIT_HAS_FAILED = 'Submit Request has failed';

BTN_SAVE_AS_DRAFT = 'Save as draft';
BTN_SUBMIT_REQUEST = 'Submit Request';

*******************************
BOOKMARKEABILITY
var optionalParam = /\((.*?)\)/g;
var namedParam    = /(\(\?)?:\w+/g;
var splatParam    = /\*\w+/g;
var escapeRegExp  = /[\-{}\[\]+?.,\\\^$|#\s]/g;
function _routeToRegExp(route) {
      route = route.replace(escapeRegExp, '\\$&')
                   .replace(optionalParam, '(?:$1)?')
                   .replace(namedParam, function(match, optional){
                     return optional ? match : '([^\/]+)';
                   })
                   .replace(splatParam, '(.*?)');
      return new RegExp('^' + route + '$');
    }

 // Given a route, and a URL fragment that it matches, return the array of
    // extracted decoded parameters. Empty or unmatched parameters will be
    // treated as `null` to normalize cross-browser behavior.
function _extractParameters(route, fragment) {
  var params = route.exec(fragment).slice(1);
  return _.map(params, function(param) {
    return param ? decodeURIComponent(param) : null;
  });
}

_routeToRegExp('movies');
_extractParameters(_routeToRegExp('movies'), 'movies');


_extractParameters(_routeToRegExp('movies/detail/:id'), 'movies/detail/5');

_extractParameters(_routeToRegExp('movies/new'), 'movies/new/');
_extractParameters(_routeToRegExp('movies/edit/:id'), 'movies/edit/fg');
-------
appRoutes: {
      'movies': 'showCollectionView',
      'movies/detail/:id': 'showDetailView',
      'movies/new': 'showFormView',
      'movies/edit/:id': 'showFormView'
    }